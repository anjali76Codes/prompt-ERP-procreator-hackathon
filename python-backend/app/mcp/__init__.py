"""Model Context Protocol — server + client wiring.

This package will host:

- `servers/` — MCP servers we expose (ERP-as-MCP for other clients to consume)
- `clients/` — MCP clients we connect to (third-party tools mounted into agents)
"""
